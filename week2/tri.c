#include <cs50.h>
#include <stdio.h>

int tri (int a, int b, int c);
int main (void)
{int a,b,c;
    {
do
{ a = get_int("a:");
 b = get_int ("b:");
 c = get_int ("c:");
    } while ( a <= 0 ||  b <= 0 || c <=0);
}
if (value==0)
{
    printf("true\n");
}
else
{
    printf("false\n");
}
}

{int tri (int a, int b, int c)
{int sumab = sum a+b;
int sumbc = sum b+c;
int sumca = sum a+c;
value=0
}
}
